"""High-level Python API.

This package will expose a small, stable surface that external Python code
(scripts, notebooks, other applications) can rely on. It wraps
:mod:`vod_analyzer.core` with convenient entrypoints and stable signatures.

Until Phase 10 the API is considered unstable and may change between minor
releases.
"""
